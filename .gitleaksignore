# Exemptions PAR EMPREINTE — le pendant historique des allowlists de
# .github/gitleaks.toml : quand un commit déjà fusionné déclenche une règle
# sans porter de valeur réelle, on exempte CE commit, jamais la règle ni le
# fichier. Une empreinte par ligne, toujours accompagnée de sa raison.
#
# On ne CITE jamais ici le texte fautif : ce fichier est balayé comme les
# autres, et recopier la forme qui déclenche la règle la déclencherait une
# seconde fois — c'est arrivé à la première rédaction de ce commentaire.

# docs/CI-CD.md, commit b0c6014 (19/08/2026) : une phrase de la doc qui NIE
# l'existence d'un secret, suivie d'un nom de variable d'environnement entre
# accents graves, a la forme exacte que la règle secret-en-dur traque — le
# mot-clé, deux-points, une valeur encadrée. Aucune valeur réelle ; vérifié
# en rejouant gitleaks 8.30.1 sur ce commit seul. Le texte du fichier a
# changé depuis, mais l'historique, lui, ne change pas — d'où l'empreinte.
b0c6014567af9f0270d9b2ec491c8d01fb704880:docs/CI-CD.md:secret-en-dur:616

# PR #121, commit ee77859 (06/09/2026) : fixture locale inventée du test
# refusant les URL Mongo avec identifiants AVANT toute connexion. Aucune
# valeur réelle. Le littéral est remplacé dans l'arbre courant ; cette seule
# empreinte historique reste exemptée, sans exclure le fichier ni la règle.
ee77859262cf768802d9b9477e55fa0407b9aab0:apps/api/src/modules/orders/public-order-admission.integration.test.ts:uri-connexion-avec-identifiants:28

# PR #125, commit 3dbc4a1 (07/09/2026) : deux fixtures loopback inventées
# prouvent le refus des URI authentifiées AVANT toute connexion. Aucun
# identifiant réel. Les littéraux sont remplacés par construction URL dans
# l'arbre courant. Seules ces deux empreintes historiques sont exemptées ;
# la règle et les fichiers restent entièrement contrôlés.
3dbc4a1c6fe549abe7b4b36f9de21c9d3b9e1f63:apps/api/src/modules/delivery/delivery-access.http.integration.test.ts:uri-connexion-avec-identifiants:78
3dbc4a1c6fe549abe7b4b36f9de21c9d3b9e1f63:apps/api/src/modules/delivery/delivery-operators.integration.test.ts:uri-connexion-avec-identifiants:55

# PR #127, commit 4d8937d (07/09/2026) : fixture locale inventée du test
# refusant toute URI authentifiée AVANT connexion. Aucun identifiant réel.
# Le littéral est remplacé par construction URL dans l'arbre courant.
# Exemption limitée à cette empreinte historique, sans exclure fichier/règle.
4d8937d18fb8b6e5443b4801034133df45b0e612:apps/api/src/modules/delivery/delivery-missions.integration.test.ts:uri-connexion-avec-identifiants:32

# PR #130, commit eb0e7d5 (07/09/2026) : fixture loopback inventée du test
# qui refuse toute cible authentifiée AVANT connexion. Aucun secret réel.
# Le littéral est remplacé par construction URL dans l'arbre courant.
# Seule cette empreinte historique est exemptée, pas le fichier ni la règle.
eb0e7d5a8a88aae8941c39def4a0e197834e533d:apps/api/src/modules/delivery/delivery-handoff.integration.test.ts:uri-connexion-avec-identifiants:33

# PR #179, commit 53aba2b (12/09/2026) : deux fixtures loopback inventées
# vérifient le refus des URL Mongo authentifiées AVANT toute connexion.
# Aucune valeur réelle ; l'arbre courant construit ces fixtures avec URL.
# Seules ces deux occurrences historiques sont exemptées, jamais les fichiers
# ni la règle de détection des URI portant des identifiants.
53aba2b750a28062d31ba07ed2d16f7c55e83f23:apps/api/src/modules/orders/dining-pricing.test.ts:uri-connexion-avec-identifiants:37
53aba2b750a28062d31ba07ed2d16f7c55e83f23:apps/api/src/modules/orders/dining.integration.test.ts:uri-connexion-avec-identifiants:40
