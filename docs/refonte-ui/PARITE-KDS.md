# Parité fonctionnelle KDS à conserver

Inventaire établi sur les sources actuelles avant leur refonte. La référence RestoPilot fixe l'aspect ; le tableau ci-dessous délimite le produit existant.

| Surface / source | Comportements à préserver | Preuve initiale |
| --- | --- | --- |
| `App.tsx`, `client.ts`, `useSession.ts`, `PinScreen.tsx` | Restauration appairage/session, scopes, PIN 4–6 chiffres, code d'appairage, erreur/restauration réessayable, suspension et verrouillage, changement établissement, déconnexion | Lecture ; tests device-boundary/API-origin/demo-retour ; parcours PIN/appairage non exécuté dans le harnais |
| `Board.tsx` | Filtres Tous/Comptoir/En ligne/Téléphone ; ordre le plus ancien d'abord ; trois statuts ; compteurs ; colonnes ≥900 px ; onglets sous 900 px | Captures 390/768/1024/1440 dans les deux thèmes ; filtre Téléphone vide puis retour Tous |
| `Toolbar.tsx`, `CompactBar` | Identité et logo du tenant, horloge, connectivité, file d'envoi, son, À lancer, paramètres, fermeture service ; filtres encore accessibles en compact | Captures et interactions paramètres ; son audiblement testé : non |
| `OrderCard.tsx`, `format.ts` | N° commande, client/type, canal, paiement, créneau, heure, minuteur, quantités, variantes, options, retraits SANS, notes, priorité et retard | Snapshot existant avec options/retraits/notes réellement rendu ; test format-options |
| `OrderCard.tsx`, `delivery-policy.ts` | `new → preparing → ready` seulement ; ready indique « Remise à confirmer par la caisse » ou attente/prise en charge livreur ; aucun bouton Servir/Remettre | Deux avancements successifs via handlers et PATCH locaux en desktop clair/sombre ; absence de bouton remise vérifiée |
| `delivery-policy.ts`, `useBoard.ts` | Livraison payée seulement ; réponse serveur avant affichage d'avancement ; un refus ne doit pas muter localement ; pas de file offline livraison ; pending empêche double geste | 10 tests delivery-policy passent ; scénario navigateur livraison non exécuté |
| `useBoard.ts`, `App.tsx` | Retraits/comptoir avancent après commit durable de file ; `pendingIds` ; statut le plus avancé ; persistance locale ; purge uniquement après sondage réussi ; rejet/stale conservés | Deux PATCH préparée/prête observés localement ; cache + panne 503 garde tickets et affiche bandeau. Reconnexion, rejeu incertain et scope concurrents non exécutés en navigateur |
| `useBoard.ts` | Trois GET de statuts, socket tenant, debounce, sondage 5s/60s, retour premier plan, lastSync, annulation des lectures d'ancien scope | Lecture ; sockets isolées/fermées pour la recette locale, fonctionnement serveur temps réel non prouvé |
| `AllDayPanel.tsx` | Cumul new+preparing, jamais ready ; groupement nom+variante ; quantités ; tri quantité puis libellé ; filtre canal partagé ; épinglage et adaptation largeur | Cumul 10 articles initial vu en desktop et onglet compact ; filtres rendus |
| `prefs.ts`, `usePrefs.ts`, `SettingsSheet.tsx`, `theme.tsx` | Sombre/clair, confort/dense, son, À lancer épinglé, splash, valeur invalide isolée, migration anciennes préférences, écritures sérialisées par scope | 12 tests prefs ; densité persistée via UI desktop ; deux thèmes capturés ; autres switches non activés dans le harnais |
| `components/primitives.tsx` | Hôte de dialogue, inert/scroll lock, fermeture Escape/retour Android, focus rendu au déclencheur, animations réduites, zones tactiles | Escape + focus retour dans les 8 scénarios nominaux ; reducedMotion=reduce ; retour Android/native non exécuté |
| `useLayout.ts`, `config.ts` | Trois colonnes ou onglets, tailles tactiles ≥56 px actions, typo éloignée, min côté, tablette/mural, épinglage À lancer même étroit | 12 tests layout ; géométrie sans débordement horizontal aux quatre tailles ; rotation native non exécutée |
| `sound.ts`, `App.tsx` | Premier chargement muet, bip nouveau une fois par ID, rappel 60s, déblocage AudioContext par geste, indisponibilité audio native explicite | Lecture ; recette son désactivé et aucune preuve acoustique |
| `ui.ts`, `format.ts` | Seuils métier 10/15 minutes et couleurs de statut distinctes de l'accent restaurant ; thème sans remontage de session/tableau | Lecture et captures des minuteurs 2/5/9/13/17/22/27 minutes ; ne pas reprendre les seuils maquette 5/10 |

Les adaptations de présentation ne doivent pas modifier les signatures, contrats, contrôleurs, hooks, calculs, transitions, requêtes, médias, journaux ou files figurant ci-dessus. Les contrôles natifs et opérationnels non exécutés restent ouverts même si le build et les captures web passent.
